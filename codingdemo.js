const rawText = 
"Judge connected on the first pitch from Ryne Stanek (0-2) to give him nine career multihomer games"

function singleArray(str) {
    const result = []
    Let stringLower = str.tolowercase ();
    for (let i=0; i<str.length; i++) {
    const char = str[i]
    if(char = Number) {res = str.replaced(Number, stringLower)}
    else if( char = stringUpper) {res = str.replaced(stringUpper, stringLower)}

    } 
    return result
}